<?xml version="1.0" encoding="utf-8"?>
<!--
  可复用的详情字段行：标签 + 内容文本（带可点击复制）。
  通过 <include android:id="@+id/row_xxx"> 引用后，
  findViewById(R.id.row_xxx).findViewById(R.id.tv_field_label/value) 取子控件。
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/ll_field_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/bg_card_white"
    android:orientation="vertical"
    android:padding="16dp">

    <TextView
        android:id="@+id/tv_field_label"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="@color/text_disabled"
        android:textSize="12sp" />

    <TextView
        android:id="@+id/tv_field_value"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:text="未设置"
        android:textColor="@color/text_title"
        android:textSize="15sp" />
</LinearLayout>
